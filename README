Build Instructions
==================
./configure && make

Build Dependencies
==================
Windows
-------
cygwin -- tar, perl, ssh, svn, gzip, etc.
The latest Windows SDK
Qt 4.4 (built with Phonon support if you want sound :P)

Mac OS X
--------
Install macports, then install libmad, taglib, libsamplerate, libfftw3
