// The first define with a value will be understood by the VersionUpdater tool
// as the current app version. It doesn't matter what you call it as long as
// you use the same name in all the places where you want the version
// automatically inserted during a build.
#ifndef VERSION_H
#define VERSION_H

#define LASTFM_FINGERPRINTER_VERSION "1.0.3.1"

#endif
